// Shared application-specific types

/**
 * Defines the possible roles for a message in the chat context.
 */
export type MessageRole = 'user' | 'assistant' | 'tool' | 'system';

// Add other shared application types here if needed 